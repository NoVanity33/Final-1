export async function onRequestPost(context){try{const{request,env}=context;if(!env.STRIPE_SECRET_KEY){return json({error:"Missing STRIPE_SECRET_KEY in Cloudflare Pages environment variables."},500)}const body=await request.json();const items=Array.isArray(body.items)?body.items:[];if(!items.length){return json({error:"Cart is empty."},400)}const origin=env.SITE_URL||new URL(request.url).origin;const params=new URLSearchParams();params.set("mode","payment");params.set("success_url",`${origin}/success.html?session_id={CHECKOUT_SESSION_ID}`);params.set("cancel_url",`${origin}/cancel.html`);params.set("shipping_address_collection[allowed_countries][0]","US");params.set("phone_number_collection[enabled]","true");params.set("allow_promotion_codes","true");params.set("billing_address_collection","auto");items.forEach((item,index)=>{if(item.productId==="shroud-tee"){params.set(`line_items[${index}][price_data][currency]`,"usd");params.set(`line_items[${index}][price_data][product_data][name]`,"The Shroud Tee");params.set(`line_items[${index}][price_data][product_data][description]`,"No Vanity 33 flagship front-and-back faith apparel");params.set(`line_items[${index}][price_data][unit_amount]`,"3700");}else{if(!item.price)throw new Error("Missing Stripe Price ID.");params.set(`line_items[${index}][price]`,item.price);}params.set(`line_items[${index}][quantity]`,String(item.quantity||1));if(item.productId)params.set(`metadata[product_${index}_id]`,item.productId);if(item.size)params.set(`metadata[product_${index}_size]`,item.size);if(item.color)params.set(`metadata[product_${index}_color]`,item.color)});const stripeRes=await fetch("https://api.stripe.com/v1/checkout/sessions",{method:"POST",headers:{"Authorization":`Bearer ${env.STRIPE_SECRET_KEY}`,"Content-Type":"application/x-www-form-urlencoded"},body:params});const session=await stripeRes.json();if(!stripeRes.ok){return json({error:session.error?.message||"Stripe checkout error."},500)}return json({url:session.url})}catch(err){return json({error:err.message||"Checkout failed."},500)}}function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json"}})}