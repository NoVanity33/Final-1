let products=[];const grid=document.getElementById('grid'),count=document.getElementById('cartCount');function render(filter='All'){grid.innerHTML=products.filter(p=>filter==='All'||p.category===filter).map(p=>`<article class="card"><img src="${p.image}" alt="${p.name}"><div class="body"><h3>${p.name}</h3><p class="price">$${Number(p.price).toFixed(2)}</p><div class="opts"><select><option>${p.color}</option></select><select>${p.sizes.map(s=>`<option>${s}</option>`).join('')}</select></div><button onclick="addCart()" class="add">🛒 Add to Cart</button><p class="purpose">Purpose: ${p.purpose}</p></div></article>`).join('')}function addCart(){let n=Number(localStorage.getItem('nv33cart')||0)+1;localStorage.setItem('nv33cart',n);count.textContent=n}document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)});fetch('data/products.json').then(r=>r.json()).then(d=>{products=d;render();count.textContent=localStorage.getItem('nv33cart')||0});