let cart=[];
function money(n){return Number(n).toFixed(2)}
function updateCart(){document.getElementById('cartCount').textContent=cart.length;const items=document.getElementById('cartItems');if(!cart.length){items.innerHTML='<p class="section-intro">Your cart is empty.</p>'}else{items.innerHTML=cart.map((x,i)=>`<div class="cart-row"><div><strong>${x.name}</strong><br>Size: ${x.size}</div><div><strong>$${money(x.price)}</strong> <button onclick="cart.splice(${i},1);updateCart()">Remove</button></div></div>`).join('')}document.getElementById('cartTotal').textContent=money(cart.reduce((s,x)=>s+x.price,0))}
document.querySelectorAll('.filters button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.card').forEach(card=>{card.style.display=(f==='All'||card.dataset.category===f)?'block':'none'})}));
document.querySelectorAll('.card .add').forEach(btn=>btn.addEventListener('click',()=>{const card=btn.closest('.card');cart.push({name:card.dataset.name,price:Number(card.dataset.price),size:card.querySelector('.size').value});updateCart();location.hash='cart'}));
updateCart();
