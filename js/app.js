const grid=document.getElementById('grid');const count=document.getElementById('cartCount');let all=[];function addCart(){let n=Number(localStorage.getItem('nv33cart')||0)+1;localStorage.setItem('nv33cart',n);count.textContent=n}function render(filter='All'){let items=all.filter(p=>filter==='All'||p.category===filter);grid.innerHTML=items.map(p=>`<article class="card"><div class="card-img"><img src="${p.image}" alt="${p.name}"></div><div class="body"><h3>${p.name}</h3><p class="price">$${Number(p.price).toFixed(2)}</p><p class="purpose">${p.purpose}</p><div class="opts"><select aria-label="Size">${p.sizes.map(s=>`<option>${s}</option>`).join('')}</select></div><button class="add" onclick="addCart()">🛒 Add to Cart</button></div></article>`).join('')}document.querySelectorAll('.filters button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)}));fetch('data/products.json').then(r=>r.json()).then(products=>{all=products;render();count.textContent=localStorage.getItem('nv33cart')||0;});